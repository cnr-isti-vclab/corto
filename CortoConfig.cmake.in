get_filename_component(CORTO_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
set(CORTO_INCLUDE_DIRS "@CONF_INCLUDE_DIRS@")
 
# Our library dependencies (contains definitions for IMPORTED targets)
#if(NOT TARGET foo AND NOT Corto_BINARY_DIR)
#  include("${CORTO_CMAKE_DIR}/FooBarTargets.cmake")
#endif()
 
# These are IMPORTED targets created by Corto.cmake
set(CORTO_LIBRARIES cortoexe)
set(CORTO_EXECUTABLE cortolib)
